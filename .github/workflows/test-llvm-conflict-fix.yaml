name: Trigger Build Taichi on Push

on:
  push:
    branches:
      - llvm-commandline-fix

jobs:
  call_build_taichi:
    uses: ./build.yml
    with:
      build_id: ${{ github.run_id }}
      nightly: true
      python: '["3.11"]'    # JSON array string
